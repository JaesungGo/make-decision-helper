# ?? ?? ?????
spring.application.name=make-decision-helper
debug=false

# Azure MySQL ?? (???? ??)
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:3306/${MYSQL_DATABASE}?useSSL=true&requireSSL=false&serverTimezone=Asia/Seoul
spring.datasource.username=${MYSQL_USERNAME}
spring.datasource.password=${MYSQL_PASSWORD}

# ???? JPA ??
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
logging.level.com.example.make_decision_helper=INFO

# WebSocket CORS (Static Web Apps URL ??)
websocket.allowed-origins=https://blue-mud-09b555900.6.azurestaticapps.net

# Connection Pool ???
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2